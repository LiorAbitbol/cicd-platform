[project]
name = "cicd-platform"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"
extend-select = ["I"] # import sorting

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["app"]
